import { createStore } from 'vuex'
import { routes } from './modules/routesModules'

const store = createStore({
  state: {
    routes
  },
  getters: {},
  mutations: {},
  actions: {}
})

export default store
